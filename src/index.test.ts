import {describe, it} from 'vitest';
import {FoosballRoutine} from "$lib/FoosballRoutine";

describe('sum test', () => {
    it('adds 1 + 2 to equal 3', () => {
        const r = new FoosballRoutine()
        r.start()
    });
});
